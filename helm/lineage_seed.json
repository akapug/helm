{
  "_source": "illustrative seed — the shipped default names no real estate. A host's true project lineage is discovered at runtime via `helm sync` (repo scan) plus operator-authored edges in registry-authored.json. Replace/extend the examples below, or start empty: {\"edges\": [], \"external\": []}.",
  "_deliberately_unlinked": [
    "sibling checkouts of one project under different roots — left unlinked until a relation is confirmed",
    "dependency-use signal (projects a repo USES vs builds — binaries consumed) — a designed, not-yet-wired discovery source"
  ],
  "edges": [
    {"src": "example-app", "rel": "descends-from", "dst": "example-root", "note": "illustrative edge — shape only, no real repo", "confirmed": false},
    {"src": "example-app", "rel": "composes", "dst": "example-lib", "note": "illustrative edge — a composed dependency", "confirmed": false}
  ],
  "external": [
    {"name": "references", "path": "~/dev/references", "note": "read-only prior art (example external root)"}
  ]
}
